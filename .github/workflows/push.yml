on: push
name: build and test
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: build
      uses: actions/npm@master
      with:
        args: ci
    - name: test
      uses: actions/npm@master
      with:
        args: t
